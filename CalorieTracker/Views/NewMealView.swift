import SwiftUI

struct NewMealView: View {
    var body: some View {
        ZStack {
            Color.white
            
            Text("New Meal")
                .foregroundStyle(.black)
        }
    }
}

#Preview {
    NewMealView()
}
