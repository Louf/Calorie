import SwiftUI

struct NewRecipeView: View {
    var body: some View {
        ZStack {
            Color.white
            
            Text("New Recipe")
                .foregroundStyle(.black)
        }
    }
}

#Preview {
    NewRecipeView()
}
